# GemPalindrome


## Installation

Add this line to your application's Gemfile:

```ruby
gem 'gem_palindrome'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install gem_palindrome
